{
	"name": "@ssj/tight-sky-9fb5",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev --test-scheduled --local-protocol https --https-key-path ./localhost-key.pem --https-cert-path ./localhost.pem",
		"start": "wrangler dev --test-scheduled"
	},
	"devDependencies": {
		"wrangler": "^4.40.1"
	},
	"dependencies": {
		"web-push": "^3.6.7"
	}
}
